# AGENTS.md

## Communication

- 답변은 한국어로 한다.

## Karpathy-Inspired Agent Guidelines

이 지침은 속도보다 신중함을 약간 더 우선한다. 오타 수정이나 명확한 한 줄 변경 같은 단순 작업에는 과하게 적용하지 말고 판단해서 진행한다.

### 1. 코딩 전에 생각하기

추측하지 않는다. 혼란을 숨기지 않는다. 트레이드오프를 드러낸다.

- 구현 전에 가정을 명시한다. 불확실하면 질문한다.
- 여러 해석이 가능하면 조용히 하나를 고르지 않고 선택지를 제시한다.
- 더 단순한 접근이 있으면 말한다.
- 이해가 안 되는 부분이 있으면 멈추고, 무엇이 불명확한지 말한 뒤 질문한다.

### 2. 단순함 우선

문제를 해결하는 최소한의 코드만 작성한다. 추측성 설계는 하지 않는다.

- 요청받지 않은 기능을 추가하지 않는다.
- 한 번만 쓰이는 코드에 추상화를 만들지 않는다.
- 요청받지 않은 유연성, 설정화, 확장성을 추가하지 않는다.
- 실제로 불가능한 시나리오를 위한 에러 처리를 만들지 않는다.
- 200줄로 쓴 코드가 50줄로 가능하면 다시 줄인다.

### 3. 외과적 변경

꼭 필요한 부분만 건드린다. 내가 만든 부산물만 정리한다.

- 주변 코드, 주석, 포맷을 임의로 개선하지 않는다.
- 망가지지 않은 코드를 리팩터링하지 않는다.
- 내 방식이 다르더라도 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 삭제하지 말고 언급한다.
- 내 변경으로 unused import, 변수, 함수가 생겼다면 제거한다.
- 기존에 있던 죽은 코드는 요청받지 않았으면 제거하지 않는다.

검증 기준: 변경된 모든 라인은 사용자 요청과 직접 연결되어야 한다.

### 4. 목표 기반 실행

성공 기준을 정의하고 검증될 때까지 반복한다.

- "validation 추가"는 "잘못된 입력 테스트를 작성하고 통과시킨다"로 바꾼다.
- "버그 수정"은 "버그를 재현하는 테스트를 작성하고 통과시킨다"로 바꾼다.
- "리팩터링"은 "변경 전후 테스트가 통과하도록 보장한다"로 바꾼다.
- 여러 단계 작업은 각 단계마다 검증 방법을 붙인 짧은 계획을 세운다.

강한 성공 기준은 에이전트가 독립적으로 반복 검증하게 만든다. 약한 기준은 지속적인 확인 요청을 만든다.

## Additional Rules

- Git/커밋 메시지 규칙은 `rules/git.md`를 따른다.
- 리뷰 단축어 규칙은 `rules/review-shortcuts.md`를 따른다.

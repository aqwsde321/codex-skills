interface:
  display_name: "Project Context"
  short_description: "Create and refresh source-grounded project docs"
  default_prompt: "Use $project-context to read this repository's existing project context; do not create or refresh files unless I explicitly request it."
policy:
  allow_implicit_invocation: true
